[project]
name = "notes-api"
version = "0.1.0"
requires-python = ">= 3.13"
dependencies = [
    "fastapi",
    "uvicorn[standard]",
]

[dependency-groups]
dev = [
    "ruff",
    "black",
    "httpx>=0.28.1",
]
